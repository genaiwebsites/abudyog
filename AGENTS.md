# Workspace Rules

## Version Control & Git Policy
- NEVER execute `git commit` or `git push` without explicit user instructions or permission.
- Always ask for explicit user confirmation before committing or pushing any code to remote repositories.
