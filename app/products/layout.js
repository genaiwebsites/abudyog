export const metadata = {
  title: 'Product Portfolio',
  description: 'Three distinct product lines spanning edible oils, animal nutrition, and premium industrial derivatives.'
};

export default function ProductsLayout({ children }) {
  return <>{children}</>;
}
